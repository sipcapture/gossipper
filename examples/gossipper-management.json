{
  "_comment": "Management UAS with admin console. ui_data_dir enables /api/v2/* and internal auth by default (settings.sqlite: users, JWT, jobs). First start seeds admin/sipcapture unless users already exist. Set auth.type to none to disable. scenario_history_keep caps archived scenario versions per id (0 = unlimited).",
  "api_addr": ":8080",
  "ui_data_dir": "/usr/local/gossipper/data",
  "scenario_history_keep": 50,
  "server": {
    "role": "management",
    "scenario_name": "management",
    "transport": "u1",
    "local_ip": "0.0.0.0",
    "local_port": 5060,
    "rate": 1,
    "max_concurrent": 2048
  }
}
